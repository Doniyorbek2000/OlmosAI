# hyworld service (scaffold)

Future TEXT/IMAGE→3D-world worker. See MODEL.md. Independent GPU service; heavy
world-generation dependencies must never be added to the main app container.
Disabled until `FEATURE_WORLD_GENERATION` is enabled.
